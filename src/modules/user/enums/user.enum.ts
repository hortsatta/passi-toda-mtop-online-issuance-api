export enum UserApprovalStatus {
  Pending = 'pending',
  Approved = 'approved',
  Rejected = 'rejected',
}

export enum UserRole {
  Admin = 'admin',
  Issuer = 'issuer',
  Member = 'member',
}

export enum UserGender {
  Male = 'male',
  Female = 'female',
}
